<Styles xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style Selector="Slider /template/ Thumb /template/ Ellipse">
        <Setter Property="Fill" Value="{DynamicResource DeepPurpleLighten3Brush}" />
        <Setter Property="Stroke" Value="{DynamicResource DeepPurpleLighten2Brush}" />
        <Setter Property="StrokeThickness" Value="1" />
    </Style>

    <Style Selector="Slider /template/ Border#TrackBackground">
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="BorderBrush" Value="{DynamicResource GreyLighten1Brush }" />
    </Style>

    <Style Selector="Slider /template/ RepeatButton#PART_DecreaseButton.repeattrack">
        <Setter Property="Background" Value="{DynamicResource DeepPurpleLighten1Brush }" />
    </Style>
    <Style Selector="Slider /template/ RepeatButton#PART_LineUpButton">
        <Setter Property="Foreground" Value="Red" />
    </Style>
    <Style Selector="ScrollBar /template/ RepeatButton > Path">
        <Setter Property="Fill" Value="{DynamicResource ThemeForegroundLowBrush}" />
    </Style>

    <Style Selector="ScrollBar /template/ RepeatButton:pointerover > Path">
        <Setter Property="Fill" Value="{DynamicResource DeepPurpleLighten1Brush}" />
    </Style>
</Styles>